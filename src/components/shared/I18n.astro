---
import { translate, translateContent } from '../../i18n';
interface Props { text: string; content?: boolean; }
const { text, content = false } = Astro.props;
---
<span data-i18n={text} data-i18n-kind={content ? 'content' : undefined}>{content ? translateContent(text) : translate(text)}</span>
